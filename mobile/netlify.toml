[build]
  base = "mobile"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback: semua rute diarahkan ke index.html (React Router)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
