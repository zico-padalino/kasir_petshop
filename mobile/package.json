{
  "name": "pet-shop-mobile",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Versi mobile (PWA) aplikasi Kasir pet Shop - database tersimpan di perangkat",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy:cloudflare": "npm run build && npx wrangler pages deploy dist --project-name=pet-shop-mobile",
    "deploy:netlify": "npm run build && npx netlify-cli deploy --prod --dir=dist"
  },
  "dependencies": {
    "@zxing/library": "^0.23.0",
    "html5-qrcode": "^2.3.8",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.26.2"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.8"
  }
}
